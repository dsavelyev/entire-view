# Entire-view

Adds an entry to the context menu of Git commits - *View Entire checkpoint* - 
that displays [Entire](https://entire.io/)'s file with the user's AI agent prompts
that were used to generate that commit, if a corresponding Entire checkpoint exists.

This very repository contains Entire checkpoints that can be viewed in this way.